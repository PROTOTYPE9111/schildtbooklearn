#include <iostream>
#include <cstdlib>
using namespace std;
int main()
{
    int result;
    result = abs(-10); //вызывается функция абс(), а возвращаемое ею значение присваивается переменной result
    cout << result;
    return 0;
}